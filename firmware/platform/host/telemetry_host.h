#ifndef MOTION_TELEMETRY_HOST_H
#define MOTION_TELEMETRY_HOST_H

#include <stdbool.h>
/* Host runner configures output before starting the scheduler. */
void host_telemetry_quiet(bool quiet);

#endif
